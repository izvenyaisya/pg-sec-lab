# 🚀 Быстрый запуск веб-интерфейса

## ✅ Фронтенд создан и готов!

### Запуск в 2 шага:

#### Терминал 1 - API Server
```bash
cd D:\projects\study\course-4\web\api
go run server.go
```

#### Терминал 2 - Frontend
```bash
cd D:\projects\study\course-4\web\frontend
npm run dev
```

Откройте http://localhost:3000

## Использование

1. **Загрузить JSON:**
   - Нажмите "Upload JSON Report"
   - Выберите файл `go/report.json` или любой другой отчет
   
2. **Просмотреть вкладки:**
   - **Overview** - статистика БД
   - **Roles** - таблица ролей с выделением опасных (superuser, bypass RLS)
   - **Findings** - список проблем безопасности с фильтрами

## Структура созданных файлов

```
web/frontend/
├── types/
│   └── report.ts              # TypeScript интерфейсы
├── lib/
│   └── api.ts                 # API клиент
├── components/
│   ├── FileUpload.tsx         # Загрузка отчетов
│   ├── OverviewTab.tsx        # Дашборд
│   ├── RolesTab.tsx           # Таблица ролей
│   └── FindingsTab.tsx        # Проблемы безопасности
└── app/
    └── page.tsx               # Главная страница
```

## Что делает каждый компонент

### FileUpload
- Загрузка JSON файлов
- Отправка на API
- Обработка ошибок

### OverviewTab
- Показывает версию PostgreSQL
- Настройки (SSL, password_encryption, etc.)
- Метрики: количество ролей, таблиц, RLS, findings

### RolesTab
- Таблица всех ролей
- Выделяет опасные роли (superuser, bypass RLS)
- Показывает количество привилегий

### FindingsTab
- Список всех проблем безопасности
- Фильтры по severity (critical, warning, info)
- Цветовое кодирование

## Готово к демонстрации! 🎉

Просто запустите оба сервиса и загрузите отчет.
