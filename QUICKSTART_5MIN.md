# 🚀 Быстрый запуск за 5 минут

## Вариант 1: Только анализ БД (1 минута)

```bash
cd go
./pg-sec-lab analyze --dsn "postgres://user:pass@host:5432/db" --out report.json
cat report.json
```

Готово! У вас есть JSON отчет о безопасности.

## Вариант 2: С веб-интерфейсом (5 минут)

### Шаг 1: Запустить API (30 сек)

```bash
cd web/api
go run server.go
```

Оставить терминал открытым. API на http://localhost:8080

### Шаг 2: Создать и запустить Frontend (4 мин)

**Новый терминал:**

```bash
cd web

# Создать Next.js проект
npx create-next-app@latest frontend \
  --typescript \
  --tailwind \
  --app \
  --no-src-dir \
  --import-alias "@/*"

cd frontend

# Установить зависимости
npm install recharts react-force-graph-2d lucide-react
```

### Шаг 3: Добавить код (2 мин)

Открыть `WEB_FRONTEND_GUIDE.md` и скопировать:

1. **types/report.ts** - создать файл, вставить код
2. **lib/api.ts** - создать файл, вставить код  
3. **components/FileUpload.tsx** - создать файл, вставить код
4. **components/OverviewTab.tsx** - создать файл, вставить код
5. **components/RolesTab.tsx** - создать файл, вставить код
6. **components/FindingsTab.tsx** - создать файл, вставить код
7. **app/page.tsx** - заменить содержимое

### Шаг 4: Запустить (10 сек)

```bash
npm run dev
```

Открыть http://localhost:3000

### Шаг 5: Использовать (10 сек)

1. Нажать "Upload JSON Report"
2. Выбрать файл `../../go/report.json`
3. Посмотреть вкладки: Overview, Roles, Findings

**Готово!** 🎉

---

## Вариант 3: Docker тесты (2 минуты)

```bash
cd go
docker-compose up --build pg-sec-lab
```

Результат: **17/17 тестов (100%)** ✅

---

## Быстрая проверка API

```bash
# Terminal 1
cd web/api
go run server.go

# Terminal 2
curl http://localhost:8080/api/health
# {"service":"PG SecureLab API","status":"ok","version":"1.0.0"}
```

---

## Структура после установки

```
course-4/
├── go/
│   ├── pg-sec-lab.exe        # CLI готов
│   └── report.json           # Сгенерированный отчет
│
└── web/
    ├── api/
    │   └── server.go         # API работает на :8080
    └── frontend/
        ├── types/
        ├── lib/
        ├── components/
        └── app/              # UI на :3000
```

---

## Troubleshooting

**API не запускается:**
```bash
cd web/api
go mod tidy
go run server.go
```

**Frontend ошибки:**
```bash
cd web/frontend
rm -rf node_modules
npm install
npm run dev
```

**Нет report.json:**
```bash
cd go
./pg-sec-lab generate --policy policy.yaml | head -20
# Создаст пример SQL для демонстрации
```

---

## Для курсовой

**Минимум (CLI + Docker):**
- CLI работает ✅
- Docker тесты 100% ✅
- Документация есть ✅

**Максимум (+ Web UI):**
- Все вышеКоличество + API + Frontend ✅
- Визуализация в браузере ✅
- Полный full-stack проект ✅

**Время на полную установку: 5-10 минут**
